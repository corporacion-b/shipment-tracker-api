[pytest]
markers =
    unit: tests without database or network access
    integration: tests that use the local test database
    contract: tests that validate an external API contract
    e2e: end-to-end flow tests
    external: tests that call external services and require --run-external
